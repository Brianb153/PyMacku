[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "pymacku"
version = "1.0.0"
description = "Simple Python wrapper for the MACKU mouse controller"
requires-python = ">=3.10"
dependencies = ["makcu"]
